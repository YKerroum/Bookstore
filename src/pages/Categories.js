import React from 'react';

const Categories = () => (
  <div className="container">
    <button type="button" className="check-status-button">Check status</button>
  </div>
);
export default Categories;
